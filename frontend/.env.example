# Optional: Set API base for non-proxied environments.
# Leave unset in dev to use Vite's '/api' proxy (configured in vite.config.ts).
# Example for direct backend access:
# VITE_API_BASE=http://127.0.0.1:8000
